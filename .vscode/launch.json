{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start Example Server",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}